rootProject.name = 'AlsonAcademy'
include ':app'

def reactNativeDir = file("../node_modules/react-native")
def reactGradle = new File(reactNativeDir, "react.gradle")
if (reactGradle.exists()) {
    apply from: reactGradle
}
