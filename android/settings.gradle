pluginManagement {
    repositories {
        google()
        mavenCentral()
        maven { url("$rootDir/../node_modules/react-native/android") } // ✅ مهم جداً
    }
}

rootProject.name = "AlsonAcademy"

apply from: file("../node_modules/react-native/react.gradle") // أو احذفها لو مش محتاج pods
